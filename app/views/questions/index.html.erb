<%- model_class = Question -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>

<%= render :partial => "shared/breadcrumbs" %>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= t('item') %></th>
      <th><%= t('aspect') %></th>
      <th><%=t 'action', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @questions.each do |question| %>
      <tr>
        <td><%= question.item %></td>
        <td><%= question.aspect.name %></td>
        <td>
          <%= link_to t('edit'), edit_question_path(question), :class => 'btn btn-mini' %>
          <%= link_to t('destroy'), question_path(question),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @questions %>

<%= link_to t('new'), new_question_path, :class => 'btn btn-primary' %>
