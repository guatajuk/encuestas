<div id="login-section" class="span4 offset4">
  <h2> <%= t('login') %> </h2>
<hr>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field input-prepend text">
      <span class="add-on"><i class="icon-envelope"></i></span>
      <%= f.email_field :email, autofocus: true , :placeholder => t('email') %>
    </div>

    <div class="field input-prepend text">
      <span class="add-on"><i class="icon-keys"></i></span>
      <%= f.password_field :password, autocomplete: "off", :placeholder => t('password') %>
    </div>

    <% if devise_mapping.rememberable? -%>
      <div class="field span5">
	<%= f.check_box :remember_me %>
	<%= f.label t('rememberme') , :class => "span10"%>
      </div>
    <% end -%>

    <div class="actions span7">
      <%= f.submit t('login')+"   >>", :class => "btn btn-info span12" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>