<fieldset>
	<% @answer_form.survey.questions.each do |question| %>
		<div class="control-group">
			<%= f.label question.item, :class => 'control-label' %>
			<div class="controls">
				<%= f.text_area :item, :class => 'text_area' %>
			</div>
		</div>
	<% end %>
</fieldset>